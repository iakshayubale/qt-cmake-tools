# Examples for qt-cmake-tools

# Include the CMake utilities
include_directories(${CMAKE_CURRENT_SOURCE_DIR}/../cmake)

# Basic Console App Example
add_subdirectory(basic-app)

# Widget App Example
add_subdirectory(widget_app)

# QML App Example
add_subdirectory(qml_app)
